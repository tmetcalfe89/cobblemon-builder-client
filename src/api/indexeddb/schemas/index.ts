import addonSchema from "./addons";
import animationSchema from "./animations";
import modelSchema from "./models";
import monsterSchema from "./monsters";
import poserSchema from "./posers";
import resolverSchema from "./resolvers";
import spawnSchema from "./spawns";
import specieSchema from "./species";
import textureSchema from "./textures";

const schemas = [
  addonSchema,
  monsterSchema,
  animationSchema,
  modelSchema,
  poserSchema,
  resolverSchema,
  spawnSchema,
  specieSchema,
  textureSchema,
];

export default schemas;
